{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "start:dev": "babel-node ./app/app",
    "build": "rm -rf ./build && mkdir build && babel --out-dir ./build --source-maps --copy-files ./app"
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "express-pino-logger": "^4.0.0",
    "express-validator": "^5.3.1",
    "http-status-codes": "^1.3.2",
    "mongoose": "^5.5.5",
    "morgan": "~1.9.0"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
